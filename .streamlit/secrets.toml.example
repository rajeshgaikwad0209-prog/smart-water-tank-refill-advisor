# Copy this file to secrets.toml (same folder) for local Streamlit secrets testing.
# secrets.toml is git-ignored and should NEVER be committed.
# On Streamlit Community Cloud, paste the same content into
# App Settings -> Secrets instead of uploading this file.

OPENAI_API_KEY = "your_api_key_here"
